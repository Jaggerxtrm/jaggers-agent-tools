{
  "active": [
    { "id": "auto-session-name", "displayName": "auto-session-name", "required": false },
    { "id": "beads", "displayName": "beads", "required": true },
    { "id": "compact-header", "displayName": "compact-header", "required": false },
    { "id": "custom-footer", "displayName": "custom-footer", "required": true },
    { "id": "custom-provider-qwen-cli", "displayName": "custom-provider-qwen-cli", "required": false },
    { "id": "git-checkpoint", "displayName": "git-checkpoint", "required": false },
    { "id": "lsp-bootstrap", "displayName": "lsp-bootstrap", "required": false },
    { "id": "serena-pool", "displayName": "serena-pool", "required": false },
    { "id": "service-skills", "displayName": "service-skills", "required": false },
    { "id": "session-flow", "displayName": "session-flow", "required": true },
    { "id": "sp-terminal-overlay", "displayName": "sp-terminal-overlay", "required": false },
    { "id": "xtrm-loader", "displayName": "xtrm-loader", "required": true },
    { "id": "xtrm-ui", "displayName": "xtrm-ui", "required": true },
    { "id": "xtprompt", "displayName": "xtprompt", "required": false }
  ],
  "disabled": {
    "quality-gates": "Hook-script lookup still targets .claude/hooks instead of the managed .xtrm/hooks layout.",
    "pi-serena-compact": "Passive legacy extension superseded by pi-serena-tools; do not enroll it as a global directory.",
    "core": "Library source lives under src/core; it is not an extension directory.",
    "gitnexus": "Retired extension; functionality is provided by the npm:pi-gitnexus package.",
    "serena": "Retired extension; functionality is provided by the npm:pi-serena-tools package."
  }
}
