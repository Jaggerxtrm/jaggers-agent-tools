{
  "id": "worktree-reap",
  "description": "Worktree capacity hygiene — kicks a detached, rate-limited `xt worktree reap` at session start so abandoned worktrees and regenerable build artifacts do not accumulate unbounded. The systemd user timer installed by `xt worktree install-timer` is the reliable out-of-band half; this hook is the opportunistic half.",
  "version": "1",
  "runtime": "claude",
  "order": 90,
  "claude": {
    "hooks": [
      {
        "event": "SessionStart",
        "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/worktree-reap-sweep.mjs",
        "timeout": 5000
      }
    ]
  }
}
