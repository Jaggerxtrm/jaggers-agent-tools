{
  "id": "inbox-reminder",
  "description": "xtrm-wiy5n.4.24: surface unread pane-scoped inbound messages to Claude on Stop (once each, anti-spin via @agent_inbox_reminded_keys). Closes the Claude/Pi inbox asymmetry — Pi polls its inbox on a 30 s cycle; Claude previously had no equivalent.",
  "version": "1",
  "runtime": "claude",
  "order": 30,
  "claude": {
    "hooks": [
      {
        "event": "Stop",
        "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/inbox-reminder-stop.mjs",
        "timeout": 5000
      }
    ]
  }
}
